services:
  app:
    build: .
    container_name: conta_check_status
    volumes:
      - ./config:/config
      - ../conta_archive/archive:/data_archive
      - ./log:/log
    ports:
      - "9000:9000"
    restart: unless-stopped

